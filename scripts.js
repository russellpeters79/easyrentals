document.addEventListener('DOMContentLoaded', () => {
    const button = document.getElementById('contactButton');
    button.addEventListener('click', () => {
        alert('Call +30 697 813 1329');
    });
});
